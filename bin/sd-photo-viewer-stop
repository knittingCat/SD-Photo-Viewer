#!/usr/bin/env bash
set -e
PID=$(lsof -ti :4173 2>/dev/null || true)
if [ -z "$PID" ]; then
  echo "sd-photo-viewer is not running on port 4173"
  exit 0
fi
kill $PID
echo "Stopped sd-photo-viewer (pid $PID)"
